# ADR-NNNN — [the decision, in a few words]

> A durable Architecture Decision Record. Lives in `docs/adr/` so that **future
> stages and reviews read it and do not re-litigate a settled decision.** One
> file per decision; never edit a decided ADR's meaning — supersede it with a new
> one and mark the old `Superseded`.

- **Status:** Proposed <!-- Proposed | Accepted | Superseded by ADR-MMMM -->
- **Date:** <!-- YYYY-MM-DD -->
- **Deciders:** <!-- who made the call -->
- **Relates to:** <!-- Spec `R#` / Plan `D#` / project name / other ADRs -->

## Context

<!-- The forces at play: the problem, the constraints, and what made this a real
     decision rather than an obvious default. -->

## Decision

<!-- What we chose, in the active voice: "We will…". -->

## Alternatives considered

<!-- Each real option and why it was rejected. A decision with no rejected
     option was an assumption, not a decision. -->

## Consequences

<!-- What becomes easier, and what becomes harder, as a result. Good and bad. -->

## Re-litigation guard

<!-- The load-bearing reason a future review should NOT re-propose the rejected
     path. This is the whole point of recording the ADR: so the same idea isn't
     re-suggested next time someone (or some agent) reviews the architecture.
     If there is no such reason — if this would be fine to revisit freely — this
     decision probably didn't need an ADR. -->
